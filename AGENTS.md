# Agent Notes

## Build Commands
- Build: `cmake --build build` (or `ninja -C build`)
- The project uses CMake with ninja generator
- No dedicated test target for this feature (desktop tests are limited)

## Lint
No dedicated lint command configured. Build uses clang warnings.